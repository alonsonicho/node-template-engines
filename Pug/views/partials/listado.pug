
<h2 style="color:crimson;">Historial</h2>

if DB_PRODUCTOS 
    div(class="table-responsive")
        table(class="table table-dark")
            tr(style="color: yellow;")
                th Nombre
                th Precio
                th URL
            each product in DB_PRODUCTOS
                tr
                    td=product.nombre
                    td=product.precio 
                    td=product.foto
else 
    h3(class="alert alert-danger") No se encontraron datos